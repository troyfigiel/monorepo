;; TODO: We will need to remove this since the notes will become part of my monorepo anyway.
;; (band-aid-git-clone "git@gitlab.com:troy.figiel/zettelkasten.git" org-roam-directory)
(org-roam-setup)
;; TODO: This will not work as is, because there are no org templates in my repo yet.
;; (band-aid-org-roam-set-templates
    ;; (expand-file-name "org/templates" user-emacs-directory))
